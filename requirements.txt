# --only-binary=:all: is required on Render free tier (0.1 CPU cannot compile from source)
# Remove this line when upgraded to Starter or above
--only-binary=:all:
--only-binary=:all:
streamlit==1.45.1
pandas==2.1.4
plotly==5.24.1
folium==0.19.7
streamlit-folium==0.25.0
duckdb==1.2.2
pyarrow==16.0.0
numpy==1.26.4
requests==2.32.3
